import {BalancePerCategoryCell} from './balance-per-category-cell';

describe('BalancePerCategoryCell', () => {
  it('should create an instance', () => {
    expect(new BalancePerCategoryCell(new Date())).toBeTruthy();
  });
});
