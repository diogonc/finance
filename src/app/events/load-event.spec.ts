/* tslint:disable:no-unused-variable */
import {LoadEvent} from './load-event';

describe('LoadEvent', () => {
  it('should create an instance', () => {
    expect(new LoadEvent()).toBeTruthy();
  });
});
