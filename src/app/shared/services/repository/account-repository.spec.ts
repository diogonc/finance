/* tslint:disable:no-unused-variable */
import {AccountRepository} from './account-repository';

describe('AccountRepository', () => {
  it('should create an instance', () => {
    expect(new AccountRepository()).toBeTruthy();
  });
});
