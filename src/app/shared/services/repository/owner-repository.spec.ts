/* tslint:disable:no-unused-variable */
import { OwnerRepository } from './owner-repository';

describe('OwnerRepository', () => {
  it('should create an instance', () => {
    expect(new OwnerRepository()).toBeTruthy();
  });
});
