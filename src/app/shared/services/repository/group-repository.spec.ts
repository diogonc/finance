/* tslint:disable:no-unused-variable */
import {GroupRepository} from './group-repository';

describe('GroupRepository', () => {
  it('should create an instance', () => {
    expect(new GroupRepository()).toBeTruthy();
  });
});
